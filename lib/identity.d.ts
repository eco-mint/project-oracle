/**
 * createIdentity generates a 32 byte seed and returns it as a
 * base64 encoded string which can be accepted as an argument to this CLI.
 */
export declare function createIdentity(): Promise<string>;
